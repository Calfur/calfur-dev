apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: default
  name: q-swiper
  labels:
    app: q-swiper

spec:
  replicas: 1
  selector:
    matchLabels:
      app: q-swiper
  template:
    metadata:
      labels:
        app: q-swiper
    spec:
      containers:
        - name: q-swiper
          image: ghcr.io/q-swiper/map-generator:6cce42a2d2f0545f3933f6e0dbce2f6a8636db8f
          ports:
            - name: web
              containerPort: 80
      imagePullSecrets:
        - name: ghcr-secret-q-swiper
