apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: star-wars-bild-countdown

spec:
  ports:
    - protocol: TCP
      name: web
      port: 80
  selector:
    app: star-wars-bild-countdown
